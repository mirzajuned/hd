$("#offer-manager-modal").html("<%= escape_javascript(render partial: 'edit.html.erb') %>");
<%= render partial: "form.js.erb" %>
start_end_date_validation();

if(<%= @offer_manager.store_id.present? %>) {
  $('#div_store').show()
} else {
  $('#div_store').hide()
}

validate_offer_form();

$("#offer_managers_facility_id").select2({disabled:'readonly'});
$("#offer_manager_service_id").select2( {dropdownParent: $('#offer-manager-modal') });
