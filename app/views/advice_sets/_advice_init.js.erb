var specialty_id = "<%= params[:specialty_id] || @available_specialties.try(:first).try(:id) %>"

var advice_sets = jQuery('#advice-set-list').dataTable({
  "bDestroy": true,
  "sPaginationType": "full_numbers",
  "bServerSide": true,
  "sAjaxSource":'/advice_sets/data.json?level=<%= @level %>&specialty_id='+specialty_id+'',
  "aoColumns": [
    {"sTitle":"Advice Sets Name","sWidth": "60%"},
    {"sTitle":"Created on","sWidth": "20%"},
    {"sTitle":"Actions","sWidth": "20%", "bSortable": false}
  ],
  "oLanguage": {
    "sZeroRecords": "No Advice Sets found",
    "sLoadingRecords": "Please wait - loading advice sets..."
  }
})

//jQuery("select").chosen({
//  'min-width': '100px',
//  'white-space': 'nowrap',
//  disable_search_threshold: 10
//})

jQuery('select[name="advice-set-list_length"]').chosen({
  'min-width': '100px',
  'white-space': 'nowrap',
  disable_search_threshold: 10
})
