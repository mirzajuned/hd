$("#manage_patient_type_id").trigger("click");

if ($(".ui-pnotify").length > 0) { $(".ui-pnotify").remove() }
notice = new PNotify({
  title: "Success",
  text: "PatientType Updated Successfully",
  type: "success"
});
notice.get().click(function(){ notice.remove() });