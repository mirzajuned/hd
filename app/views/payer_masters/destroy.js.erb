if($('#payer_master_table_content .facility-list').length > 0){
  $("#manage_payer_master_id").trigger('click')
} else {
  $('.btn-refresh-payer-list').trigger('click')
}
$("#contact-modal").modal('hide')
