$('#payer_master_table_content').html("<%= j render partial: 'payer_list.html.erb' %>")

// BACK Button
$(".back-btn").show()
$(".back-btn").on('click', function(e){
  e.preventDefault()
  $("#manage_payer_master_id").trigger('click')
  $(this).hide()
})
