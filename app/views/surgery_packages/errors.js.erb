$('.ui-pnotify').remove();
notice = new PNotify({
  title: 'Error',
  text: "<%= @duplicate_codes.empty? ? 'Something went wrong' : @duplicate_codes.uniq.join + " Code already taken" %>",
  type: 'error'
});

notice.get().click(function(){
  notice.remove()
});
