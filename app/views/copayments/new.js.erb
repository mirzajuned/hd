$("#insurance-modal").html("<%= escape_javascript(render 'new') %>")
$("#insurance-modal").modal('show')


function copay_new() {
  $("#new_copayment").validate({
    rules: {
      
    },
    messages: {
      
    },
    errorElement: 'div',
    errorPlacement: function (error, element) {
      error.insertAfter(element);
    }
  });
}
copay_new()

$.validator.addMethod("CopaymentAmount", $.validator.methods.number,
  "Amount Should be Number");

$.validator.addMethod("CopaymentAmountPresence", $.validator.methods.required,
  "Amount Cannot be blank");

$.validator.addClassRules("copay_amount", { CopaymentAmount: true, CopaymentAmountPresence: true});