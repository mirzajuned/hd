$("#counsellor-modal").html("<%= escape_javascript(render 'show.html.erb') %>");

if("<%= params[:action] %>" != "show"){
  $("#btn-appointment-management-refresh").trigger('click') // for Converted Queue Refresh
}