<div class="row">
  <div class="col-md-12">
    <strong>Radiology</strong>
  </div>
</div>
<% if @counsellor_record.present? && @radiology_investigations.count > 0 %>
  <div class="row">
    <table class="table">
      <thead>
        <tr>
          <th style="width: 20%;">Name</th>
          <th class="text-center">Advised</th>
          <th class="text-center">Counselled</th>
          <th class="text-center">Agreed</th>
          <th class="text-center">Payment Taken</th>
          <th class="text-center">Declined</th>
          <th class="text-center">Performed</th>
        </tr>
      </thead>
      <tbody>
        <% @radiology_investigations.each do |radiology_investigation| %>
          <tr style="font-size: 12px;">
            <td>
              <b style="color: #333;"><%= radiology_investigation.investigationname.to_s + ', ' + radiology_investigation.get_label_for_radiology_option(radiology_investigation.radiologyoptions) %></b>
            </td>
            <td class="text-center">
              <% if radiology_investigation.is_advised %>
                <%= radiology_investigation.advised_by %><br/><%= radiology_investigation.try(:advised_datetime).try(:strftime, '%d/%m/%Y') %>
              <% else %>
                -
              <% end %>
            </td>
            <td class="text-center">
              <% if radiology_investigation.is_counselled %>
                <%= radiology_investigation.counselled_by %><br/><%= radiology_investigation.try(:counselled_datetime).try(:strftime, '%d/%m/%Y') %>
              <% else %>
                -
              <% end %>
            </td>
            <td class="text-center">
              <% if radiology_investigation.has_agreed %>
                <%= radiology_investigation.agreed_by %><br/><%= radiology_investigation.try(:agreed_datetime).try(:strftime, '%d/%m/%Y') %>
              <% else %>
                -
              <% end %>
            </td>
            <td class="text-center">
              <% if radiology_investigation.payment_taken %>
                <%= radiology_investigation.payment_taken_by %><br/><%= radiology_investigation.try(:payment_taken_datetime).try(:strftime, '%d/%m/%Y') %>
              <% else %>
                -
              <% end %>
            </td>
            <td class="text-center">
              <% if radiology_investigation.has_declined %>
                <%= radiology_investigation.declined_by %><br/><%= radiology_investigation.try(:declined_datetime).try(:strftime, '%d/%m/%Y') %>
              <% else %>
                -
              <% end %>
            </td>
            <td class="text-center">
              <% if radiology_investigation.is_performed %>
                <%= radiology_investigation.performed_by %><br/><%= radiology_investigation.try(:performed_datetime).try(:strftime, '%d/%m/%Y') %>
              <% else %>
                -
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <div class="row">
    <div class="col-md-12">
      <i>No Radiology Investigation</i>
    </div>
  </div>
<% end %>