<% if current_facility.ipd_clinical_workflow %>
  <%= render partial: 'inpatients/ot/clinical_data_rows.html.erb', locals: { ot_list: ot_list } %>
<% else %>
  <%= render partial: 'inpatients/ot/non_clinical_data_rows.html.erb', locals: { ot_list: ot_list } %>
<% end %>

<% unless ot_list.count > 0 %>
  <div class='row no_margin summary_ot'>
    <div class="col-md-12 text-center">
      <b>Nothing to Show</b>
    </div>
  </div>
<% end %>
