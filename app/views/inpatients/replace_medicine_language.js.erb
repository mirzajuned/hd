<% if @ipdrecord.department.present? %>

  <% if @ipdrecord.specialty_id == "309988001" %>
    $('.replace_ipd_medicine').html("<%= escape_javascript(render partial: 'inpatients/ophthal_replace_medicine_language.html', locals: {discharge_note: @discharge_note,advice_language: @advice_language, admission_id: @ipdrecord.admission_id}) %>")

  <% else %>
    $('.replace_ipd_medicine').html("<%= escape_javascript(render partial: 'inpatients/ortho_replace_medicine_language.html', locals: {discharge_note: @discharge_note,advice_language: @advice_language, admission_id: @ipdrecord.admission_id }) %>")

  <% end %>

  $('.replace_advice_set').html("<%= escape_javascript(render partial: 'inpatients/replace_advice.html', locals: { operative_note: @operative_note,advice_language: @advice_language}) %>")
<% end %>