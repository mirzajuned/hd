var invoice_templates = jQuery('#invoice-template-list').dataTable({
    "bDestroy": true,
    "sPaginationType": "full_numbers",
    "bServerSide": true,
    "sAjaxSource":"/invoice_templates/data.json" ,
    "aoColumns": [
        {"sTitle":"Invoice Template Name","sWidth": "80%"},
        {"sTitle":"Actions","sWidth": "20%", "bSortable": false}
    ],
    "oLanguage": {
        "sZeroRecords": "No invoice templates found",
        "sLoadingRecords": "Please wait - loading invoice templates..."
    }
})
jQuery("select[name='invoice-template-list_length']").chosen({
    'min-width': '100px',
    'white-space': 'nowrap',
    disable_search_threshold: 10
})

jQuery('select[name="certificate-template-list_length"]').chosen({
  'min-width': '100px',
  'white-space': 'nowrap',
  disable_search_threshold: 10
})