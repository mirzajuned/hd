jQuery("#custom_consents").html("<%= escape_javascript(render 'index') %>");
<%= render partial: 'custom_init.js' %>

$('#specialty_id').on('change', function(){
  let specialty_id = this.value;
  let level = '<%= @level %>'
  $.ajax({
    type: "GET",
    url: '/custom_consents',
    data: { specialty_id: specialty_id, level: level, remote: "true" },
  });
});