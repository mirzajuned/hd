<%= javascript_include_tag "application-loggedin", "data-turbolinks-track" => true %>
<div class="modal-dialog contact-dialog" style="width:50%;">
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal">&times;</button>
      <h4 class="modal-title">New Contact Form</h4>
    </div>
    <%= form_for @contact, method: "post", remote: true do |f| %>
      <%= f.hidden_field :organisation_id, value: @current_facility.organisation_id.to_s %>
      <%= render partial: 'form.html.erb', locals: { f: f } %>
    <% end %>
  </div>
</div>