$("#btn-investigation-details-refresh").click();
$("#btn-investigation-management-refresh").click();
if ($('.ui-pnotify').length > 0) { $('.ui-pnotify').remove() } notice = new PNotify({ title: 'Success', text: 'All caught up! No <%= @task.capitalize %> left  :)', type: 'success' }); notice.get().click(function(){ notice.remove() });