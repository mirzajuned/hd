jQuery("#fill-lab-record").html("<%= escape_javascript(render 'show') %>")
//jQuery("#fill-lab-record").modal("hide")
var current_user_role_ids = <%= current_user.role_ids %>;
var current_user_department_ids = <%=raw current_user.department_ids %>;
if ((current_user_role_ids.includes(158965000)) || current_user_department_ids.includes("309988001") || current_user_department_ids.includes("309989009")){
    $("#btn-investigation-details-refresh").trigger('click')
}
else{
    $("#btn-investigation-management-refresh").trigger('click')
}


if($(".patient-timeline-panel").length > 0){
  $("#btn-timeline-refresh").trigger("click")
}