<% if current_organisation_setting.try(:organisation_account_sid).blank? %>
	$("#communication-whatsapp-number-modal").html("<%= escape_javascript(render 'communication_settings/approved_numbers/checked_account_sid.html.erb') %>")
<% else %>
	$("#communication-whatsapp-number-modal").html("<%= escape_javascript(render 'communication_settings/approved_numbers/add_communication_number.html.erb') %>")
<% end %>