$("#service-master-modal").modal("hide")
if($("#pricing_master_table").length > 0){
  $('.btn-refresh-pricing-list').trigger('click')
} else {
  $("#manage_pricing_master_id").trigger('click')
}

notice = new PNotify({
  title: 'Pricing Master',
  text: 'Updated Successfully',
  icon: 'fa fa-check',
  type: 'success'
})
notice.get().click(function(){ notice.remove() });
