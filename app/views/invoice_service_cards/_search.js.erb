$(".search-service-card").on('keyup', function(){
  var search = $(this).val()
  // if(search.trim().length > 2){
    $.ajax({
      type: "GET",
      dataType: "script",
      data: {
        remote: "true",          
        search: search
      },
      url: '/invoice_service_cards/search',
      success: function(){
        
      }
    })
  // }
})