<%= form_for finance_tpa, :url => finance_tpa_masters_path, :authenticity_token => false do |f| %>
  <%= render partial: '/finance/tpa_masters/corporate_details_form.html.erb', locals: {f: f, index: (Time.current.to_f.round(3)*1000).to_i } %>
<%end%>
