

//$("#expense_dashboard").addClass("active")


$(".finance-expense").on('click',function(e){
  e.preventDefault();
  $(".finance-expense").removeClass("active")
  $(this).addClass("active")

//  var targeturl = "expenses/dashboard"
//
//
//  jQuery.ajax({
//    type: "GET",
//    dataType: "script",
//    url: targeturl,
//    remote: true
//  });

})


//jQuery("#manage_certificate_templates_id").on('click',function(e) {
//  jQuery.ajax({
//    type: "GET",
//    dataType: "script",
//    url: '/user_notes_templates'
//  });
//});


  var expenseItemsHeight, sidebarHeight;
  expenseItemsHeight = void 0;
  sidebarHeight = void 0;
  sidebarHeight = $(window).height() - $('.navbar').height() -15;
  expenseItemsHeight = sidebarHeight - $('.toolbar').height() - 15;
  $('.sidebar-main').height(sidebarHeight);
  $('.expenses_content').height(expenseItemsHeight);
  $(".trimmed_expenses_content").height(expenseItemsHeight);
  $(".show_expense").height(expenseItemsHeight);


$(window).resize(function() {
  var expenseItemsHeight, sidebarHeight;
  sidebarHeight = $(window).height() - $(".navbar").height() - 15;
  expenseItemsHeight = sidebarHeight - $(".toolbar").height() - 15;
  $(".sidebar-main").height(sidebarHeight);
  $(".expenses_content").height(expenseItemsHeight);
  $(".trimmed_expenses_content").height(expenseItemsHeight);
  return $(".show_expense").height(expenseItemsHeight);
});