<div class="panel">
  <div class="panel-heading" style="overflow:auto;">
  	<% level = @level.present? ? @level : 'user' %>
    <div class="pull-right"><%= link_to new_procedure_note_path(level: level),:data => {:remote => true,'toggle' =>  "modal", 'target' => '#invoice-modal'}, :class => 'btn btn-success'  do%><i class="fa fa-plus"></i> Add Procedure Note<% end %></div>
  </div>
  <div class="panel-body">
    <table class="table table-striped table-primary table-bordered" id="procedure-note-list">

    </table>
  </div>
</div>