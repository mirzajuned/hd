<% @no_data = "<b class='no-data'>No Data</b>".html_safe %>

<% @name = sub_referral_type.try(:name) %>
<% @name = @no_data unless @name.present? %>

<% @comment = sub_referral_type.try(:comment) %>
<% @comment = @no_data unless @comment.present? %>

<% @speciality = sub_referral_type.try(:speciality) %>
<% @speciality = @no_data unless @speciality.present? %>

<% @designation = sub_referral_type.try(:designation) %>
<% @designation = @no_data unless @designation.present? %>

<% @existing_patient = sub_referral_type.try(:existing_patient) %>
<% @existing_patient = @no_data unless @existing_patient.present? %>

<% @relation = sub_referral_type.try(:relation) %>
<% @relation = @no_data unless @relation.present? %>

<% @campaign_type = Global.sub_referral_type.campaign_type[sub_referral_type.try(:campaign_type)] %>
<% @campaign_type = @no_data unless @campaign_type.present? %>

<% @camp_date = sub_referral_type.try(:camp_date).try(:strftime, '%d %b %Y') %>
<% @camp_date = @no_data unless @camp_date.present? %>

<% @doctor_name = sub_referral_type.try(:doctor) %>
<% @doctor_name = @no_data unless @doctor_name.present? %>

<% @department = sub_referral_type.try(:department) %>
<% @department = @no_data unless @department.present? %>

<% @agency_name = sub_referral_type.try(:agency_name) %>
<% @agency_name = @no_data unless @agency_name.present? %>

<% @email = sub_referral_type.try(:email) %>
<% @email = @no_data unless @email.present? %>

<% @mobile_number = sub_referral_type.try(:mobile_number) %>
<% @mobile_number = @no_data unless @mobile_number.present? %>

<% @location = sub_referral_type.try(:location) %>
<% @location = @no_data unless @location.present? %>

<% @city = sub_referral_type.try(:city) %>
<% @city = @no_data unless @city.present? %>

<% @state = sub_referral_type.try(:state) %>
<% @state = @no_data unless @state.present? %>

<%= render partial: 'sub_referral_types/views/' + sub_referral_type.referral_type.label + '.html.erb' %>

<style type="text/css">
  .no-data {
    font-size: 12px;
    opacity: 0.5;
    font-style: italic;
  }
</style>
