var certificate_templates = jQuery('#certificate-template-list').dataTable({
  "bDestroy": true,
  "sPaginationType": "full_numbers",
  "bServerSide": true,
  "sAjaxSource":"/user_notes_templates/data.json?level=<%= @level %>&type=<%= @type %>" ,
  "aoColumns": [
    {"sTitle": "<%= Global.user_notes_types['template_types'].find {|x| x[:label] == @type}[:name] + ' ' + 'Name' %>","sWidth": "80%"},
    {"sTitle":"Actions","sWidth": "20%", "bSortable": false}
  ],
  "oLanguage": {
    "sZeroRecords": "No certificate templates found",
    "sLoadingRecords": "Please wait - loading certificate templates..."
  }
})
//jQuery("select").chosen({
//  'min-width': '100px',
//  'white-space': 'nowrap',
//  disable_search_threshold: 10
//})
jQuery('select[name="certificate-template-list_length"]').chosen({
  'min-width': '100px',
  'white-space': 'nowrap',
  disable_search_threshold: 10
})