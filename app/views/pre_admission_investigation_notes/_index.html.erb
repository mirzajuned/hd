<div class="panel">
  <div class="panel-heading" style="overflow:auto;">
  	<% level = @level.present? ? @level : 'user' %>
    <div class="pull-right"><%= link_to new_pre_admission_investigation_note_path(level: level),:data => {:remote => true,'toggle' =>  "modal", 'target' => '#invoice-modal'}, :class => 'btn btn-success'  do%><i class="fa fa-plus"></i> Add Note<% end %></div>
  </div>
  <div class="panel-body">
    <table class="table table-striped table-primary table-bordered" id="pre-admission-investigation-note-list">

    </table>
  </div>
</div>