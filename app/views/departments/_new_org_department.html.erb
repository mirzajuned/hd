<%= f.select(:facility_department_id, Department.all.collect { |p| [p.name, p.id] }, {prompt: 'Select Department'}, {:class => 'form-control'}) %>
<%= f.hidden_field :user_id, {:class => 'form-control'} %>