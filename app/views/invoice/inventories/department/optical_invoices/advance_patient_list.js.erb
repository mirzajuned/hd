jQuery("#inventory-modal").html("<%= escape_javascript(render '/invoice/inventories/department/optical_invoices/advance_patient_list') %>")
jQuery("#inventory-modal").modal("show")


$("#searchCheckoutLog").on("keyup", function () {
  query = $(this).val()

//  if (query.length > 2) {
    $.ajax({
      type : 'get',
      url: '/invoice/inventories/department/optical/search_optical_advance_log',
      data: {q: query, remote: "true" },
    })
//  }
})