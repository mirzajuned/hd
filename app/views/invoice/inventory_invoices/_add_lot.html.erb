<%= form_for inventory_invoice, :url => invoice_inventory_invoices_path, :authenticity_token => false do |f| %>

  <%= render partial: '/invoice/inventory_invoices/add_lot_form.html.erb', locals: { f: f, variant: @variant, item: @item, lot: @lot, index: nil } %>

<% end %>