$("#invoice-detail-modal").modal('hide')
<% if params[:revert_path] == 'settle_invoices' %>
  $.get($('.settle_store_bills').attr('href'))
<% end %>
$('.inventory-items #<%=@inventory_invoice.id%>').trigger('click')
var preview = "<%= escape_javascript(render '/invoice/inventory_invoices/print_preview.html.erb') %>"
$("#invoice-modal").html(preview)
$("#free-invoice-inventory-modal").html(preview)

adjust_height()
function adjust_height(){
  inventoryHeight = $(window).height() - $(".navbar").height() - 3
  toolbar2Height = $(".inventory-toolbar2").height()
  $(".inventory-show-panel").height(inventoryHeight - toolbar2Height)
}
$('.search-button').trigger('click')