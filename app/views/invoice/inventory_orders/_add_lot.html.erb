<%= form_for inventory_order, :url => invoice_inventory_orders_path, :authenticity_token => false do |f| %>

  <%= render partial: '/invoice/inventory_orders/add_lot_form.html.erb', locals: { f: f, variant: @variant, item: @item, lot: @lot, index: nil } %>

<% end %>