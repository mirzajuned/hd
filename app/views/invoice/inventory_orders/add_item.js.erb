$('#inventory-item-modal').modal('hide')
$('.invoice-table-body').append($("<%= escape_javascript(render partial: '/invoice/inventory_orders/add_item.html.erb', locals: {inventory_order: @inventory_order} ) %>").html());

<%= render partial: 'invoice/inventory_orders/form.js.erb' %>

var tax_group = {}
tax_group['tax_rates'] = <%= raw @tax_rate_details.to_json %>
tax_group['id'] = '<%=@tax_group.id%>'
tax_group['group_rate'] = '<%=@tax_group.rate%>'
tax_breakup['<%=@item.id%>'] = tax_group

$('body').addClass('modal-open');