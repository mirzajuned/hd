<%= form_for inventory_order, :url => invoice_inventory_orders_path, :authenticity_token => false do |f| %>

  <%= render partial: '/invoice/inventory_orders/add_item_form.html.erb', locals: { f: f, item: @item, index: nil } %>

<% end %>