<% if current_facility.department_ids.include?('225738002') %>
  <div class="col-sm-12">
    <%# unless @admission_list_view.nil? %>
      <%#= link_to ipd_patients_scheduleot_path(:admission_id => @admission_list_view.admission_id, :patient_id => @patient.id, ottime: "current_time"), id: 'schedule-ot-btn', class: "btn btn-primary btn-primary-transparent btn-xs", :data => {:remote => true, 'toggle' =>  "modal", 'target' => '#templates-modal'} do %> <!-- Schedule OT --><%# end %>
    <%# else %>
      <%#= link_to ipd_patients_scheduleot_path(:patient_id => @patient.id, ottime: "current_time"), class: "btn btn-primary btn-primary-transparent btn-xs", :data => {:remote => true, 'toggle' =>  "modal", 'target' => '#templates-modal'} do %> <!-- Schedule OT --><%# end %>
    <%# end %>
    <%= link_to new_ot_path(patient_id: @patient.id, date: Date.current, time: Time.current, direct: true), class: "btn btn-primary btn-primary-transparent btn-xs", :data => {:remote => true, 'toggle' =>  "modal", 'target' => '#ot-modal'} do %> Schedule OT<% end %>
  </div>
<% end %>  