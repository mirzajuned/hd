$('.diagnoses-tab-link').on('click', function(e) {
  e.preventDefault();
  $('.diagnoses-tab').click();
});

$('.procedures-tab-link').on('click', function(e) {
  e.preventDefault();
  $('.procedures-tab').click();
});

$('.investigations-tab-link').on('click', function(e) {
  e.preventDefault();
  $('.investigations-tab').click();
});

$('.prescriptions-tab-link').on('click', function(e) {
  e.preventDefault();
  $('.prescriptions-tab').click();
});

$('.glasses-tab-link').on('click', function(e) {
  e.preventDefault();
  $('.glasses-tab').click();
});

$('#medicalPrescriptionsTable').on('click', '.prescription-details', function() {
  let modalId = $(this).attr('data-modal-id')
  $(`#${modalId}`).modal('show')
});

$('#opticalPrescriptionsTable').on('click', '.optical-prescription-details', function() {
  let modalId = $(this).attr('data-modal-id')
  $(`#${modalId}`).modal('show')
});
<% if current_facility.show_finances %>
  <% if @refund_payments.size > 5 %>
    $('#refundPayments-appointment-overview-tab').DataTable({
      sDom: "<'top'p>",
      ordering: false,
      displayStart: 0,
      displayLength: 5,
    });
  <% end %>

  <% if @advance_payments.size > 5 %>
    $('#advancePayments-appointment-overview-tab').DataTable({
      sDom: "<'top'p>",
      ordering: false,
      displayStart: 0,
      displayLength: 5,
    });
  <% end %>
<% end %>
