if ("<%=@counter%>" == 0){
  jQuery(".outside_organisation_referral_added").prepend('<%= escape_javascript(render partial: "opd_referrals/add_outside_organisation_referral", locals: {i: @counter}) %>')
} else {
  jQuery(".outside_organisation_referral_added> .outside-organisation-referral:last").after('<%= escape_javascript(render partial: "opd_referrals/add_outside_organisation_referral", locals: {i: @counter}) %>')
}

datepicker_options = {
  dateFormat: "dd/mm/yy",
  minDate: 0
};

jQuery('.outsideorg-referraldate-datepicker').datepicker(datepicker_options);
