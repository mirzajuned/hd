jQuery("#requisition-order-modal").modal("hide")
$("#cancelModal").modal('hide')
$('.inventory-show-panel').text('')
$('.modal-backdrop').remove();

$(".inventory-table-body").html("<%= escape_javascript(render partial: 'inventory/order/requisition_received/index_body.html.erb') %>")

$('.inventory-items .inventory-list-row').unbind()
$('.inventory-items .inventory-list-row').on('click', function(e){
  e.preventDefault();
  var _id =  $(this).attr('id');
  $('.inventory-list-row').css('background-color', '')
  $(this).css('background-color', '#f0f0f0')
  $.ajax({
    type: 'GET',
    data: {
      store_id: '<%= @inventory_store.id %>',
      remote: true
    },
    url: '/inventory/order/requisition_received/'+_id,
    success: function(){
    }
  })
})
