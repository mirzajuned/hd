$("#inventory-modal").html('').modal("hide")
var row = $('.purchase-order-list #<%= @purchase_order.id %>')
row.find('.total-cost b').text("<%= @purchase_order.net_amount %>")
row.find('.order-vendor').text("<%= @vendor&.name %>")
row.find('.order-vendor').text("<%= @purchase_order.vendor_location_address %>")
row.find('.notes b').text("<%= @purchase_order.note %>")
row.trigger('click')