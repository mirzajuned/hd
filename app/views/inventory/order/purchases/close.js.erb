$("#closeModal").modal('hide')
var row = $('.purchase-order-list #<%= @purchase_order.id %>')
row.trigger('click')
<% if @purchase_order.closed? %>
  row.find('.order-status b').text('closed')
  notice = new PNotify({ title: 'Closed', text: 'This Purchase is closed', type: 'success' });
<% else %>
  notice = new PNotify({ title: 'Error', text: 'Please try again', type: 'error' });
<% end %>
notice.get().click(function(){ notice.remove() });
$('#confirmation-modal').modal('hide')
$('.modal-backdrop').hide();