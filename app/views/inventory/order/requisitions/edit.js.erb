$("#inventory-modal").html("<%= escape_javascript(render '/inventory/order/requisitions/new.html.erb') %>")
$("#inventory-modal").modal("show")

var counter = $('#tbody_medication tr').length

  if (counter == 0) {
    $(".medication-table-body-added").html("<%= escape_javascript(render 'item_list.html.erb') %>")
  } else {
    $('#medicationTable > tbody:last').append("<%= escape_javascript(render 'item_list.html.erb') %>")
  }

<%= render partial: 'new.js.erb' %>

