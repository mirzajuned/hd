function validate_inventory_terms_and_conditions_form() {
  $('#inventory_terms_and_conditions_form').validate({
    rules: {
    },
    messages: {
    },
    errorPlacement: function (error, element) {
      var elem = $(element);
      error.insertAfter(element);
      if(elem.hasClass("select2-hidden-accessible")) {
        element = $("#select2-" + elem.attr("id") + "-container").parent();
        error.insertAfter(element);
      } else {
        error.insertAfter(element);
      }
    }
  });
}


validate_inventory_terms_and_conditions_form()

$('#inventory_terms_and_condition_type').select2()

$.validator.addClassRules("fullname", { required: true, checkfullname: true});

$.validator.addMethod("checkDescription", function(value) {
  if (value.trim() == '') {
    return false
  } else {
    return true
  }
}, "Required");

$.validator.addClassRules("description", { required: true, checkDescription: true});