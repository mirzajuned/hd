<%= form_for item, :url => inventory_items_path, :authenticity_token => false do |f| %>
  <%= render partial: '/inventory/items/custom_fields_form.html.erb', locals: {f: f, index: (Time.current.to_f.round(3)*1000).to_i } %>
<%end%>
