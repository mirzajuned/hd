$('.item-custom-fields').append($("<%= escape_javascript(render partial: 'inventory/items/custom_fields', locals: {item: @item} ) %>").html());
<!--or-->
<!--data = "<%#= escape_javascript(render partial: 'inventory/items/custom_fields', locals: {item: @item} ) %>"-->
<!--var el_to_add;-->
<!--el_to_add = $(data).html();-->
<!--$('.item-custom-fields').append(el_to_add);-->


$('.item-custom-field-value').select2({
  tags: true,
  tokenSeparators: [','],
  placeholder: "Enter possible values",
  val: []
})

$('.remove-item-custom-field').on('click',function () {
  $(this).parents('.item-custom-field').remove()
})

var btn = $('.custom_fields_button')
btn.remove()
$('.item-custom-fields').append(btn)
