<%= javascript_include_tag "application-loggedin", "data-turbolinks-track" => true %>

<%= form_for Inventory::Item::Optical::Contact.build_with_custom_field, as: :inventory_item,:url => inventory_items_path,:format => :js ,:method => :post,:remote=>true, :validate => true do |f| %>

  <%= render partial: 'inventory/items/item_categories/optical_item_form', locals: { f: f, category: "optical_contact" } %>

<% end %>
<script type="text/javascript">

  datepicker_options = {
    changeMonth: true,
    changeYear: true,
    dateFormat: "yy-mm-dd",
    minDate: -0
    //showButtonPanel: true,
  };

  $('.datepicker').datepicker(datepicker_options);

  $(".dispensing-unit-dd").select2();

  // Tooltip
  $(".hg-tooltip").tipsy();
  $(".hg-tooltip").on('click', function(){
    $(".tipsy").hide();
  })
</script>
