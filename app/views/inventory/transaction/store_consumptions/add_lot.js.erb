var class_val = '<%= params[:class_val] %>'
if (class_val != '') {
	$('.'+class_val).remove()
}

var id = '<%= params[:lot_id] %>'
// $('#'+id).css('background-color', '#cfe8d2')
// $('#'+id).addClass('disabled_row')

$('.transaction-table-body').append($("<%= escape_javascript(render partial: 'inventory/transaction/store_consumptions/add_lot.html.erb', locals: { store_consumption: @store_consumption } ) %>").html());

$("#add_lot_modal").modal("hide")
$("#add-unit-lot-modal").modal("hide")

$('#consumed_user_id').select2()
<%= render partial: 'form.js.erb' %>
