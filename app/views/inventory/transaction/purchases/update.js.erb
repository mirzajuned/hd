$("#inventory-modal").html('').modal("hide")
var row = $('.purchase-transaction-list #<%= @purchase_transaction.id %>')
row.find('.total-cost b').text("<%= @purchase_transaction.total_cost %>")
row.find('.notes b').text("<%= @purchase_transaction.note %>")
row.trigger('click')