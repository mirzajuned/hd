$(".purchase-transactions-body").html("<%= escape_javascript(render partial: 'inventory/transaction/purchase_bills/purchase_transaction.html.erb') %>")

  var vendor_gst_no = "<%= @vendor.gst_number if @vendor.present? %>"
  var vendor_name = "<%= @vendor.name if @vendor.present? %>"
  $(".vendor_gst_number").val(vendor_gst_no)
  $("#inventory_transaction_purchase_bill_vendor_name").val(vendor_name)

<% if params[:action_type] == 'edit' &&  @purchase_bill.vendor_id.to_s == params[:vendor_id] && params[:create_against] == @purchase_bill.create_against %>
  $('.purchase-bill-table tbody').append("<%= escape_javascript(render partial: 'inventory/transaction/purchase_bills/lots.html.erb', locals: { purchase_bill: @purchase_bill } ) %>");
<% end %>
check_tax()
