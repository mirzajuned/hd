var row = $('.purchase-transaction-list #<%= @purchase_bill.id %>')
row.trigger('click')

<% if @purchase_bill.approved? %>
  row.find('.status b').text('approved')
  notice = new PNotify({ title: 'Approved', text: 'This Purchase Bill is approved', type: 'success' });
<% else %>
  notice = new PNotify({ title: 'Error', text: 'Please try again', type: 'error' });
<% end %>
notice.get().click(function(){ notice.remove() });