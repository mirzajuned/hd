var row = $('.inventory-items #<%= @stock_opening.id.to_s%>')
row.trigger('click')

<% if @stock_opening.status_approved? %>
  row.find('.status b').text('Approved')
  notice = new PNotify({ title: 'Approved', text: 'This Stock Opeing is approved', type: 'success' });
<% else %>
  notice = new PNotify({ title: 'Error', text: 'Please try again', type: 'error' });
<% end %>
notice.get().click(function(){ notice.remove() });