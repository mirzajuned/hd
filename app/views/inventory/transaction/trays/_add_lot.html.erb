<%= form_for tray_transaction, :url => inventory_transaction_trays_path, :authenticity_token => false do |f| %>

  <%= render partial: 'inventory/transaction/trays/add_lot_form.html.erb', locals: { f: f, variant: @variant, item: @item, lot: @lot, index: nil } %>

<% end %>