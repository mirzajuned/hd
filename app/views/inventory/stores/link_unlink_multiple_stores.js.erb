$("#store-modal").html("<%= escape_javascript(render 'link_unlink_multiple_stores.html.erb') %>")

$("#store_ids").select2()

function link_unlink_store() {

  $("#save_link_unlink_multiple_stores").validate({
    rules: {
      "store_ids[]": {
        required: true,
      },
    },
    messages: {
      "store_ids[]": {
        required: "Select Atleast One Store"
      }
    },
  });
}
link_unlink_store()