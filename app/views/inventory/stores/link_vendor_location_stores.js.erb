$("#store-modal").html("<%= escape_javascript(render 'link_vendor_location_stores.html.erb') %>")

<%# $("#vendor_location_ids").select2() %>

function link_unlink_vendor() {

  $("#save_multiple_vendor_location").validate({
    rules: {
      // "vendor_location_ids[]": {
        // required: true,
      // },
    },
    messages: {
      // "vendor_ids[]": {
      //   required: "Select Atleast One Vendor"
      // }
    },
  });
}
link_unlink_vendor()