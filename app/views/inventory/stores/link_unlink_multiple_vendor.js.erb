$("#store-modal").html("<%= escape_javascript(render 'link_unlink_multiple_vendor.html.erb') %>")

$("#vendor_ids").select2()

function link_unlink_vendor() {

  $("#save_link_unlink_multiple_vendor").validate({
    rules: {
      "vendor_ids[]": {
        required: true,
      },
    },
    messages: {
      "vendor_ids[]": {
        required: "Select Atleast One Vendor"
      }
    },
  });
}
link_unlink_vendor()