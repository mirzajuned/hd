<%= simple_form_for(@inventory_item_stock) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :unit_price %>
    <%= f.input :quantity %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
