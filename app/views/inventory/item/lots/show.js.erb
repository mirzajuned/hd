$(".pharmacy-panel").html("").hide()
$(".store-content-panel").show()

jQuery(".inventory-show-panel").html("<%= escape_javascript(render partial: 'inventory/item/lots/show') %>")

adjust_height()
function adjust_height(){
  inventoryHeight = $(window).height() - $(".navbar").height() - 3
  toolbar2Height = $(".inventory-toolbar2").height()
  $(".inventory-show-panel").height(inventoryHeight - toolbar2Height)
}

$('.blockLot').click(function(e){
  e.preventDefault();
  $("#closeModal").modal('show');
})

function validateMessage() {
  $("#new-message-form").validate({
    rules: {
      "message": {
        required: true
      }
    },
    messages: {
      "message": {
        required: "Message Required."
      }
    },
    errorPlacement: function (error, element) {
      error.insertAfter(element);
    }
  })
}
$('.unblockLot').click(function(e){
  e.preventDefault();
  $("#closeModal1").modal('show');
})
