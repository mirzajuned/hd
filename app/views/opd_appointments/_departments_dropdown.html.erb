<!-- Not in use -->
<%
   departments_array = @departments.map { |department| [department.name, department.id] }
   departments_array = [["All Departments","0"]] + departments_array
%>
<% if departments_array.count > 2 %>
    <%= select_tag("department-filter", options_for_select(departments_array,current_user.department_id),{:class => 'form-control'}) %>
<% else %>
    <%= hidden_field_tag("department-filter",current_user.department_id) %>
<% end %>