jQuery(document).ready(function() {
    jQuery(".rom-options").hide();
    jQuery(".distalneurovasculardeficit-options").hide();
    jQuery("input[name='opdrecord[rom]']").on('change', function() {
        if (this.value === "Painful") {
            jQuery(".rom-options").show();
        }
        else if (this.value === "Painless") {
            jQuery(".rom-options").show();
        }
    });
    jQuery(".skincondition_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "Normal") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_skincondition").val("");
            } else {
                jQuery(".skincondition_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_skincondition").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "Normal") {
            jQuery.each(jQuery(".skincondition_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "Normal" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_skincondition").val("");
                }
            });
        }
    });
    jQuery(".tenderness_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_tenderness").val("");
            } else {
                jQuery(".tenderness_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_tenderness").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".tenderness_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_tenderness").val("");
                }
            });
        }
    });
    jQuery(".deformity_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_deformity").val("");
            } else {
                jQuery(".deformity_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_deformity").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".deformity_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_deformity").val("");
                }
            });
        }
    });
    jQuery(".supracondylarridges_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "Normal") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_supracondylarridges").val("");
            } else {
                jQuery(".supracondylarridges_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_supracondylarridges").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "Normal") {
            jQuery.each(jQuery(".supracondylarridges_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "Normal" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_supracondylarridges").val("");
                }
            });
        }
    });
    jQuery("input[name='opdrecord[distalneurovasculardeficit]']").on('change', function() {
        if (this.value == "Present") {
            jQuery(".distalneurovasculardeficit-options").show();
        }
        else if (this.value == "Absent") {
            jQuery(".distalneurovasculardeficit-options").hide();
        }
    });
    jQuery(".swelling_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_swelling").val("");
            } else {
                jQuery(".swelling_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_swelling").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".swelling_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_swelling").val("");
                }
            });
        }
    });
    jQuery(".abnormalmobilitylocation_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_abnormalmobilitylocation").val("");
            } else {
                jQuery(".abnormalmobilitylocation_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_abnormalmobilitylocation").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".abnormalmobilitylocation_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_abnormalmobilitylocation").val("");
                }
            });
        }
    });
    jQuery("#opdrecord_anthropometryheight, #opdrecord_anthropometryweight").on('change', function() {
        if ((jQuery("#opdrecord_anthropometryheight").val().length > 0) && (jQuery("#opdrecord_anthropometryweight").val().length > 0)) {
            var height = jQuery("#opdrecord_anthropometryheight").val() / 100;
            var weight = jQuery("#opdrecord_anthropometryweight").val();
            var bmi = "";
            bmi = weight / Math.pow(height, 2);
            jQuery("#opdrecord_anthropometrybmi").val(bmi.toFixed(1));
        }
    });
    var min , max;
    <% @right_flexion_minmax = Array.new %>
    <% @right_flexion_minmax=@opdrecord.romsliderrangerightextensionflexion %>
    <% if @right_flexion_minmax.nil? %>
        min = 0;
        max = 150;
   <%else%>
   <% @right_flexion_minmax=@right_flexion_minmax.split(":") %>
        min = <%= @right_flexion_minmax[0] %>
        max = <%= @right_flexion_minmax[1] %>
  <% end %>


    jQuery( "#rom-slider-range-right-extension-flexion" ).slider({    
        range: true,
        min: 0,
        max: 150,
        values: [ min, max ],
        slide: function( event, ui ) {
            jQuery( "#opdrecord_romsliderrangerightextensionflexion" ).val( ui.values[0]+" : "+ui.values[1]); 
        }
    });


    <% @left_flexion_minmax = Array.new %>
    <% @left_flexion_minmax=@opdrecord.romsliderrangeleftextensionflexion %>
    <% if @left_flexion_minmax.nil? %>
        min = 0;
        max = 150;
   <%else%>
   <% @left_flexion_minmax=@left_flexion_minmax.split(":") %>
        min = <%= @left_flexion_minmax[0] %>
        max = <%= @left_flexion_minmax[1] %>
  <% end %>


    jQuery( "#rom-slider-range-left-extension-flexion" ).slider({
        range: true,
        min: 0,
        max: 150,
        values: [ min, max ],
        slide: function( event, ui ) {
            jQuery( "#opdrecord_romsliderrangeleftextensionflexion" ).val( ui.values[ 0 ] + " : " + ui.values[ 1 ] );
        }
    });



    <% @right_supination_pronation = Array.new %>
    <% @right_supination_pronation=@opdrecord.romsliderrangerightsupinationpronation %>
    <% if @right_supination_pronation.nil? %>
        min = -90;
        max = 80;
   <%else%>
   <% @right_supination_pronation=@right_supination_pronation.split(":") %>
        min = <%= @right_supination_pronation[0] %>
        max = <%= @right_supination_pronation[1] %>
  <% end %>
    jQuery( "#rom-slider-range-right-supination-pronation" ).slider({
        range: true,
        min: -90,
        max: 80,
        values: [ min, max ],
        slide: function( event, ui ) {
            jQuery( "#opdrecord_romsliderrangerightsupinationpronation" ).val( ui.values[ 0 ] + " : " + ui.values[ 1 ] );
        }
    });

    <% @left_supination_pronation = Array.new %>
    <% @left_supination_pronation=@opdrecord.romsliderrangeleftsupinationpronation %>
    <% if @left_supination_pronation.nil? %>
        min = -90;
        max = 80;
   <%else%>
   <% @left_supination_pronation=@left_supination_pronation.split(":") %>
        min = <%= @left_supination_pronation[0] %>
        max = <%= @left_supination_pronation[1] %>
  <% end %>

    jQuery( "#rom-slider-range-left-supination-pronation" ).slider({
        range: true,
        min: -90,
        max: 80,
        values: [ min, max ],
        slide: function( event, ui ) {
            jQuery( "#opdrecord_romsliderrangeleftsupinationpronation" ).val( ui.values[ 0 ] + " : " + ui.values[ 1 ] );
        }
    });
    jQuery( "#opdrecord_romsliderrangerightextensionflexion" ).val( jQuery( "#rom-slider-range-right-extension-flexion" ).slider( "values", 0 ) +   " : " + jQuery( "#rom-slider-range-right-extension-flexion" ).slider( "values", 1 ) );
    jQuery( "#opdrecord_romsliderrangeleftextensionflexion" ).val( jQuery( "#rom-slider-range-left-extension-flexion" ).slider( "values", 0 ) +   " : " + jQuery( "#rom-slider-range-left-extension-flexion" ).slider( "values", 1 ) );
    jQuery( "#opdrecord_romsliderrangerightsupinationpronation" ).val( jQuery( "#rom-slider-range-right-supination-pronation" ).slider( "values", 0 ) +   " : " + jQuery( "#rom-slider-range-right-supination-pronation" ).slider( "values", 1 ) );
    jQuery( "#opdrecord_romsliderrangeleftsupinationpronation" ).val( jQuery( "#rom-slider-range-left-supination-pronation" ).slider( "values", 0 ) +   " : " + jQuery( "#rom-slider-range-left-supination-pronation" ).slider( "values", 1 ) );

});


function distalneurovasculardeficit(){
  if ($("input[name='opdrecord[distalneurovasculardeficit]']").val() == "Present") {
    jQuery(".distalneurovasculardeficit-options").show();
  }
  else if ($("input[name='opdrecord[distalneurovasculardeficit]']").val() == "Absent") {
    jQuery(".distalneurovasculardeficit_options").hide();
  }
}

distalneurovasculardeficit();