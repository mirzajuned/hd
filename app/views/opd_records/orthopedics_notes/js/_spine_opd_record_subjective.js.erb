jQuery(document).ready(function() {
    <%= render partial: "opd_records/orthopedics_notes/js/regiontemplates_soap_opdrecord_subjective_common.js" %>
    jQuery(".upperlimbcomplaints_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_upperlimbcomplaints").val("");
            } else {
                jQuery(".upperlimbcomplaints_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_upperlimbcomplaints").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".upperlimbcomplaints_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_upperlimbcomplaints").val("");
                }
            });
        }
    });
    jQuery(".lowerlimbcomplaints_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_lowerlimbcomplaints").val("");
            } else {
                jQuery(".lowerlimbcomplaints_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_lowerlimbcomplaints").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".lowerlimbcomplaints_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_lowerlimbcomplaints").val("");
                }
            });
        }
    });
    jQuery(".assistance_buttons").on('click', function() {
        var clickedbutton = this;
        if (jQuery(clickedbutton).attr("input-value") === "None") {
            if (jQuery(clickedbutton).hasClass("btn-darkblue")) {
                jQuery(clickedbutton).removeClass("btn-darkblue");
                jQuery("#opdrecord_assistance").val("");
            } else {
                jQuery(".assistance_buttons").removeClass("btn-darkblue");
                jQuery("#opdrecord_assistance").val("");
            }
        } else if (jQuery(clickedbutton).attr("input-value") !== "None") {
            jQuery.each(jQuery(".assistance_buttons"), function(index, button) {
                if (jQuery(button).attr("input-value") === "None" && jQuery(button).hasClass("btn-darkblue")) {
                    jQuery(button).removeClass("btn-darkblue");
                    jQuery("#opdrecord_assistance").val("");
                }
            });
        }
    });
});




