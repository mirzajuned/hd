$("#user-modal").html("<%= escape_javascript(render 'link_unlink_multiple_facilities.html.erb') %>")
$('#facility_ids').select2();

function link_facilities() {
  $("#link_facilities_save").validate({
    rules: {
      "facility_ids[]": {
        required: true,
      },
    },
    messages: {
      "facility_ids[]": {
        required: "Select any Facility first"
      }
    }
  });
}
link_facilities()