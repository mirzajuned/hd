
<%= select_tag "user[facility_ids]",options_for_select( Facility.where(:organisation_id => @user.organisation_id,:is_active => true, :department_ids.in=> [@department_id]).collect {|p| [ p.name, p.id ] } ,@user.facility_ids.map(&:to_s)), {:class => 'form-control facilities_name',:multiple => true, 'data-placeholder' => 'Choose Facilities',:include_blank=> false} %>
