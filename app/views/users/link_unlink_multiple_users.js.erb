$("#user-modal").html("<%= escape_javascript(render 'link_unlink_multiple_users.html.erb') %>")

$('#user_ids').select2({
  placeholder: 'Search by Name/EmployeeID/Designation'
});

function link_user() {
  $("#link_user_save").validate({
    rules: {
      "user_ids[]": {
        required: true,
      },
    },
    messages: {
      "user_ids[]": {
        required: "Select any user first"
      }
    }
    // ,
    // errorPlacement: function (error, element) {
    //   var elem = $(element);
    //   if(elem.hasClass("select2-hidden-accessible")) {
    //     element = $("#select2-" + elem.attr("id") + "-container").parent();
    //     error.insertAfter(element);
    //   } else {
    //     error.insertAfter(element);
    //   }
    // }
  });
}
link_user()