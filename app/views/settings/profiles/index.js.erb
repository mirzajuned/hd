// <!-- In Use -->
jQuery(".contentpanel").html("<%= escape_javascript(render 'index.html.erb') %>")

jQuery('#userdob-datepicker-inline').datepicker({
  dateFormat: "dd/mm/yy",
  changeMonth: true,
  changeYear: true,
  yearRange: "c-120:c",
  onClose: function(){
    //workaround to trigger javascript validation
    $(this).focusout();
  }
})