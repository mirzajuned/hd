
    var seatmap = "seatmap";
    var counter = '<%= count %>';
    var seatmapid = seatmap + counter;
    jQuery('#<%= ward.name %>').seatCharts({
        map: [
            'aaaaaaaaaaaa'
        ],
        seats: {
            a: {
                price   : 99.99,
                classes : 'front-seat' //your custom CSS class
            }
        },
        naming: {
            top: false,
            left: false
        }
    });

