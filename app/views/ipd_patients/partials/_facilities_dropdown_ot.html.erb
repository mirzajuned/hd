<% if @facilities_array.count > 1 %>
    <%= select_tag("facility-filter-ot", options_for_select(@facilities_array, current_facility.id), {:class => 'form-control'}) %> <!-- @current_facility[0] gives name and id both  -->
<% else %>
    <%= hidden_field_tag("facility-filter-ot", @current_facility_array[0][1]) %> <!-- @current_facility[0][1] gives only id-->
<% end %>