if($('.ot-panel').length > 0){
  var doctor_id = $("#sidebar_doctors_ot .active").find('a').attr('data-doctor-id')
  var active_tab = $(".tab_active").find(".active").attr('data-tab-id')
  if("<%= @active_ot == @ot %>" == "true"){
    refresh_ot_management(doctor_id,active_tab,"")
  } else {
    refresh_ot_management(doctor_id,active_tab,"<%= @active_ot.id.to_s %>")
  }
}

if($('.ot-calendar-panel').length > 0){
  if("<%= @active_ot == @ot %>" == "true"){
    $("#ot-calendar").fullCalendar('refetchEvents');
    $("#modalCalendarOtDetails").modal("hide")
  } else {
    $("#ot-calendar").fullCalendar('refetchEvents');
  }
}

if($(".admission-panel").length > 0){
  $(".admission_today").find('.active').find("[data-admission-id='<%= @ot.try(:admission_id).to_s %>']").trigger('click');
}

if($('.admission-calendar-panel').length > 0){
  $("#ipd-calendar").fullCalendar('refetchEvents');  
}


if($(".appointment-panel").length > 0){
  // Refresh for Appoitnment
  $("#appointment-ot-list").html("<%= escape_javascript(render 'outpatients/appointment/appointment_details/appointment_ot_list.html.erb') %>")
}

if($('.appointment-calendar-panel').length > 0){
  // Refresh for Appoitnment
  $("#appointment-ot-list").html("<%= escape_javascript(render 'outpatients/appointment/appointment_details/appointment_ot_list.html.erb') %>")
}


if($(".patient-timeline-panel").length > 0){
  $("#btn-timeline-refresh").trigger('click')
}