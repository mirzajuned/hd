$("#insurance-modal").html("<%= escape_javascript(render 'inpatient/insurance/courier/show') %>")
$("#insurance-modal").modal("show")

// Admission Management
<% if params[:action] == "create" %> // To Avoid Unnecessary Query Call on Update
  if($(".admission-panel").length > 0){
    $(".admission_today").find('.active').find("[data-admission-id='<%= @courier.admission_id.to_s %>']").trigger('click');
  }
<% end %>

$("#btn-tpa-management-refresh").trigger("click")