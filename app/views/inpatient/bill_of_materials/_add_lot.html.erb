<%= form_for bom_transaction, :url => inpatient_bill_of_materials_path, :authenticity_token => false do |f| %>

  <%= render partial: 'add_lot_form.html.erb', locals: { f: f, variant: @variant, item: @item, lot: @lot, index: nil } %>

<% end %>