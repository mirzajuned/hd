$("#pre-anaesthesia-modal").html("<%= escape_javascript(render 'edit.html.erb') %>")
$("#pre-anaesthesia-modal").modal("show")
datepicker_options = {
  dateFormat: "dd/mm/yy"
}
$(".datepicker").datepicker(datepicker_options);