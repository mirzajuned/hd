<% @speciality_folder_name = speciality %>
<%= render partial: 'inpatient/ipd_record/discharge_note/' + "#{speciality}_notes" + '/view_body.html.erb', locals: { view: view, mail_job: true, discharge_note: ipd_record.discharge_note, patient: patient, admission: ipd_record.admission, ipd_record: ipd_record, tpa: tpa, current_facility: current_facility, case_sheet: case_sheet, advice_language: advice_language, language_flag: language_flag } %>

<style type="text/css">
  .master-row * {
    font-size: 9px;
  }

  .pcs-entity-title span {
    font-size: 16px;
    text-transform: uppercase;
  }
</style>