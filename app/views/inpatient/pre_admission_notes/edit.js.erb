$("#pre-admission-modal").html("<%= escape_javascript(render 'edit.html.erb') %>")
$("#pre-admission-modal").modal("show")
datepicker_options = {
  dateFormat: "dd/mm/yy"
}
$(".datepicker").datepicker(datepicker_options);