$("#upload_via_excel_medication").modal('hide');
$("#upload_via_excel_medication").html('');
var level = "<%= @level %>";
$("#add_excel").removeClass("active");
var url = '/medication_kits/user_med_list';
  jQuery.ajax({
    type: "GET",
    dataType: "script",
    url: url,
    data: {remote: "true",level: level }
  });
