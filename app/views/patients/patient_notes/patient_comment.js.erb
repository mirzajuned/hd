jQuery("#patient-comment").html("<%= escape_javascript(render 'patients/patient_notes/patient_comments') %>")
jQuery("#patient-comment").modal("show")

refresh_stuff_opd();
function refresh_stuff_opd(){
  window.Showday.fetchAppointmentData();
}

// Admission Management
if($(".admission-panel").length > 0){
  $(".admission_today").find('.active').find("[data-admission-id='<%= @admission.id.to_s %>']").trigger('click');
}
