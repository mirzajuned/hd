if ($('.ui-pnotify').length > 0) { $('.ui-pnotify').remove() }

notice = new PNotify({
  title: 'Error',
  text: "<%= sanitize @admission.errors.full_messages.join('<br>') %>",
  type: 'error'
});

notice.get().click(function(){ notice.remove() });
