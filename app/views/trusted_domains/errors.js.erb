$('.ui-pnotify').remove();

notice = new PNotify({
  title: 'Error',
  text: "<%= @trusted_domain.errors.empty? ? 'Something went wrong' : @trusted_domain.errors.full_messages.join('\n') %>",
  type: 'error'
});

notice.get().click(function(){
  notice.remove()
});
