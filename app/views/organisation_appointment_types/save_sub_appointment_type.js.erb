PNotify.removeAll(); 

notice = new PNotify({
            title: 'Success',
            text: 'Appointment types saved successfully',
            type: 'success' 
          }); 

notice.get().click(function(){ notice.remove() }); 

$("#organisation_sub_appointments").html("<%= escape_javascript(render 'sub_appointment_type') %>");