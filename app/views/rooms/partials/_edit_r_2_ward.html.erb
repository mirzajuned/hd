<%# ward.rooms.each_with_index do |room, room_index| %>
  <!-- <div class="panel-group panel-group-dark addRoomsAccordion add-room-for-ward-<%= room_index %>" id="add-room-for-ward">
    <div class="panel panel-default">

      <div class="panel-heading">
        <h4 class="panel-title">
          <a data-toggle="collapse" class="collapsed" data-parent="#addRoomsAccordion" href="#addRoomsAccordion<%= room_index + 1 %>" data-addRoomsAccordion="<%= room_index + 1 %>">
            Room <%= room_index + 1 %>
          </a>
        </h4>
      </div>
      <div id="addRoomsAccordion<%= room_index + 1 %>" class="panel-collapse collapse" style="height: 0px;" data-addRoomsAccordion="<%= room_index + 1 %>">

        <div class="panel-body">
          <div class="ward_room_details_<%= room_index + 1 %>" id="ward_room_details_<%= room_index + 1 %>">

            <div class="row mb10">

              <div class="col-md-3">
              </div>

              <div class="col-md-3 required">
                <label>Name</label>
              </div>

              <div class="col-md-2 required">
                <label>Code</label>
              </div>

              <div class="col-md-4">
              </div>

            </div>

            <div class="row mb10">

              <div class="col-md-2">
              </div>

              <div class="col-md-3">
                <%= text_field_tag "ward[rooms_attributes][#{room_index}][name]", room.name, {:maxlength => 20, :size => 20, :class => 'form-control room_name'} %>
                <%= hidden_field_tag "ward[rooms_attributes][#{room_index}][organisation_id]", @organisation_id.to_s, {:class => 'form-control ward_room_organisation_id'} %>
                <%= hidden_field_tag "ward[rooms_attributes][#{room_index}][facility_id]", room.facility_id.to_s, {:class => 'form-control ward_room_facility_id'} %>
                <%= hidden_field_tag "ward[rooms_attributes][#{room_index}][_destroy]", 0, {:class => 'form-control ward_room_destory'} %>

                <%= hidden_field_tag "ward[rooms_attributes][#{room_index}][id]", room.id.to_s, {:class => 'form-control ward_room_id id'} %>
              </div>
              <div class="col-md-2">
                <%= text_field_tag "ward[rooms_attributes][#{room_index}][code]", room.code, {:maxlength => 4, :size => 5, :class => 'form-control room_code', :readonly => "readonly"} %>
              </div>

              <div class="col-md-2">
              </div>

            </div>

          </div>

          <span class="btn btn-xs btn-lightblue room_add_beds" id="room_add_beds" data-room_add_beds="<%= room_index %>"><i class="glyphicon glyphicon-plus"></i> Bed</span>

          <div class="add_b_2_room" id="add_b_2_room_<%= room_index %>">
                <% beds_hash = (room.beds.group_by{ |group| group.sequence } ) %>
                <%= render 'beds/partials/edit_b_2_room', { room: room, room_index: room_index, beds_hash: beds_hash } %>
          </div>

          <span class="btn btn-xs btn-danger pull-right ward_remove_room ward_remove_room-<%= room_index %>" id="ward_remove_room_<%= room_index %>" name="remove-room-button" data-ward_remove_room="<%= room_index %>"><i class="glyphicon glyphicon-remove"></i> Room <%= room_index + 1 %></span>
        </div>
      </div>

    </div>
  </div> -->
<%# end %>

