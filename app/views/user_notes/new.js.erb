jQuery("#invoice-modal").html("<%= escape_javascript(render 'form') %>")
jQuery("#invoice-modal").modal("show")
jQuery('#user-note-datepicker-inline').datepicker({
  dateFormat: "dd/mm/yy",
  changeMonth: true,
  changeYear: true,
  yearRange: "c-120:c",
  onClose: function(){
    //workaround to trigger javascript validation
    $(this).focusout();
  }
})
$("#user-note-datepicker-inline").datepicker().datepicker("setDate", new Date());

function new_user_note() {
  $("#new_user_note").validate({
    rules: {
      "user_note[category]": {
        required: true,
      },
      "user_note[date_of_issue]": {
        required: true,
      },
      "user_note[medical_subject]": {
        required: true,
      },


    },
    messages: {
      "user_note[category]": {
        required: "This field is required."
      },
      "user_note[date_of_issue]": {
        required: "This field is required."
      },
      "user_note[medical_subject]": {
        required: "This field is required."
      },
    },
  });

}
new_user_note()

var summernote_options = {
  toolbar: [
    ['style', ['bold', 'italic', 'underline', 'clear']],
    ['fontsize', ['fontsize']],
    ['height', ['height']],
    ['fontname', ['fontname']],
  ],height: 200,
  fontNames: ['Arial', 'Arial Black', 'Comic Sans MS', 'Courier New','Georgia','Times New Roman','Palatino Linotype','Lucida Console','Verdana','Trebuchet MS','Helvetica','English111 Adagio BT Regular','Impact']
};
$('#user_note_medical_body').summernote(summernote_options);


