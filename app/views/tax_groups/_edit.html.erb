<%= javascript_include_tag "application-loggedin", "data-turbolinks-track" => true %>
<div class="modal-dialog" style="width:45%;">
  <!-- Modal content-->
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal">&times;</button>
      <h4 class="modal-title">Edit Tax Group</h4>
    </div>
    <%= form_for @tax_group, class: 'tax-group-form', remote: true do |f| %>
      <%= f.hidden_field :user_id, value: @tax_group.user_id.to_s %>
      <%= f.hidden_field :organisation_id, value: @tax_group.organisation_id.to_s %>
      <%= f.hidden_field :country_id, value: @tax_group.country_id.to_s %>
      <div class="modal-body">
        <%= render partial: 'form.html.erb', locals: { f: f } %>
      </div>
      <div class="modal-footer">
        <%= f.submit "Save", class: 'btn btn-success btn-save-tax-group', remote: true, 'data-disable-with' => "Please wait..", onclick: 'edit_tax_group()' %>
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    <% end %>
  </div>
</div>
