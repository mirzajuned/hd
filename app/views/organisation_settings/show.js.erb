$("#organisation-modal").html("<%= escape_javascript(render 'show.html.erb') %>")

if ('<%= @organisation_setting.organisation_notification_enabled %>' === 'true') {
  $('.notification_button').show();
  $('.notification-settings').show();
} else {
  $('.notification_button').hide();
  $('.notification-settings').hide();
}
